version: "3.8"

services:

  llm:
    image: ollama/ollama
    container_name: llm_server
    ports:
      - "11434:11434"
    volumes:
      - ollama_data:/root/.ollama
    restart: "no"

volumes:
  ollama_data:
